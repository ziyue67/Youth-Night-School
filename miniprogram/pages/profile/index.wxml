<view class="container">
  <view class="profile-section">
    <image class="avatar" src="{{userInfo.avatarUrl || '../../images/icons/avatar.png'}}" mode="aspectFit"></image>
    <text class="welcome-text">��������</text>
  </view>

  <view class="info-section">
    <view class="info-item">
      <text class="label">�ǳ�</text>
      <text class="value">{{userInfo.nickName || '�ഺҹУѧԱ'}}</text>
    </view>
    
    <view class="info-item">
      <text class="label">�ֻ���</text>
      <text class="value">{{phoneNumber || 'δ��'}}</text>
      <button 
        wx:if="{{!phoneNumber}}" 
        class="bind-btn"
        open-type="getPhoneNumber"
        bindgetphonenumber="handleGetPhoneNumber">
        ���ֻ���
      </button>
    </view>
  </view>

  <view class="tip-section" wx:if="{{!phoneNumber}}">
    <text class="tip-text">���ֻ��ź�����ܸ�����Ի�����</text>
  </view>
</view>